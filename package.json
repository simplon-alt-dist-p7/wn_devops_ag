{
  "name": "world-news",
  "version": "1.0.0",
  "description": "...",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "start:all": "concurrently -n \"READER,WRITER\" -c \"blue,magenta\" \"npm run dev --prefix wn-falcon-reader\" \"npm run dev --prefix wn-falcon-writer\"",
    "test:all": "npm run test --prefix wn-falcon-reader/server && npm run test --prefix wn-falcon-reader/client && npm run test --prefix wn-falcon-writer/server && npm run test --prefix wn-falcon-writer/client",
    "lint:all": "npm run lint --prefix wn-falcon-reader/server && npm run lint --prefix wn-falcon-reader/client && npm run lint --prefix wn-falcon-writer/server && npm run lint --prefix wn-falcon-writer/client"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.2.3",
    "concurrently": "^9.2.1",
    "eslint": "^9.39.2",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.5.0",
    "globals": "^17.3.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1",
    "typescript-eslint": "^8.56.0"
  },
  "lint-staged": {
    "wn-falcon-reader/**/*.{ts,tsx,js}": [
      "eslint --fix",
      "prettier --write"
    ],
    "wn-falcon-writer/**/*.{ts,tsx,js}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml}": [
      "prettier --write"
    ]
  }
}
